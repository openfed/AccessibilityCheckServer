/** Environment-specific configuration for the front-end. */
const config = {
  // URL of the API server.
  apiServerUrl: 'http://localhost:3000',
  wsServerUrl: 'ws://localhost:3000'
};

export default config;
